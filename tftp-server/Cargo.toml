[package]
name = "tftp-server"
version = "0.1.0"
edition = "2018"

[lib]
name = "tftp_server"

[[bin]]
name = "tftp-server"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
shared = { path = "../shared" }
